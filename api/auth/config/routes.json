{
  "routes": [
    {
      "method": "POST",
      "path": "/auth",
      "handler": "auth.sendCode",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/auth/verify",
      "handler": "auth.verifyCode",
      "config": {
        "policies": []
      }
    }
  ]
}
